[phases.setup]
nixPkgs = ["nodejs_22", "chromium", "python3", "vips", "pkg-config", "gcc", "gnumake"]

[phases.install]
cmds = ["PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm install"]

[start]
cmd = "PUPPETEER_EXECUTABLE_PATH=$(which chromium) node_modules/.bin/tsx src/index.js"
